SELECT CON_ID, PDBINC PDBINC_KEY, INCSCN, ERSCN, DECODE(STATUS, 1, 'CURRENT', 2, 'PARENT', 'ORPHAN') STATUS FROM X$KCPDBINC WHERE STATUS IN (1, 2) AND NOT EXISTS (SELECT 1 FROM V$DATABASE WHERE CDB = 'NO') ORDER BY CON_ID, ERSCN DESC