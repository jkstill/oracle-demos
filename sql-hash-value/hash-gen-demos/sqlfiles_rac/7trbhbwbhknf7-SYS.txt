select sd.inst_id, ts.tsnam, segment_file, segment_block, extent_size, current_users, total_extents, total_blocks, used_extents, used_blocks, free_extents, free_blocks, added_extents, extent_hits, freed_extents, free_requests, max_size, max_blocks, max_used_size, max_used_blocks, max_sort_size, max_sort_blocks, relative_fno, sd.ts#, sd.con_id, sd.is_local_temp from x$ktstssd sd, x$kccts ts where (sd.con_id is null or sd.con_id = ts.con_id) and sd.ts# = ts.tstsn